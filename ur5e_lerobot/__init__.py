"""ur5e_lerobot — UR5e + AmazingHand integration for LeRobot.

Arm: controlled via ROS 2 (UR driver). Hand: controlled via a TCP socket
(see hand.AmazingHandClient). The LeRobot Robot adapter (Phase 2) composes the two.
"""
